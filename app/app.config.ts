export default defineAppConfig({
  ui: {
    colors: {
      primary: 'paper',
      neutral: 'paper'
    },
    button: {
      defaultVariants: {
        color: 'neutral'
      }
    },
    prose: {
      a: {
        base: 'text-paper-900 underline border-b-0 font-medium rounded-xs'
      }
    }
  }
})
