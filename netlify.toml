# coming-soon branch: serve the static holding page, no Nuxt build.
# Revert to main as Netlify's production branch to launch the real site.
[build]
  command = "echo 'holding page: no build'"
  publish = "holding"
